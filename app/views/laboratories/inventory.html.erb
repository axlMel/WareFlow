<div data-controller="activation" class="space-y-6">

  <!-- GRID INVENTARIO -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <%= render "devices_table" %>
    <%= render "sims_table" %>
  </div>

  <!-- PANEL ACTIVACIÓN -->
  <%= render "activation_panel" %>
  <!-- HISTORIAL RECIENTE -->
  <%= render "recent_connections" %>
</div>